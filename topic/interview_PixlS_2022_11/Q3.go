package interview_PixlS_2022_11

func Solution3() {
	// Multiple choice task
	// There are some question we have to remind.

	// 1.
	// n1 := []int{1, 2, 3, 4}
	// n1 = append(n1, 100)
	// fmt.Println(len(n1), cap(n1))
	// Ans: output 5, 8

	// 2.
	// How is the string data type represented?
	// A: It uses bytes and runes to encode byte character values.
	// B: It uses bytes and runes to encode anonymous character values.
	// C: It uses runes but does not use bytes to represent the string data type.
	// Ans: A?

	// 3.
	// When are function closures executed?
	// A: When the package is initialized, such functions are executed.
	// B: When the package is called, such functions are executed.
	// C: When all closures are valid in the code.
	// Ans: B?

	// 4. What is the difference between the array and slice types?
	// Ans: Array has a fixed length; slice has a dynamic length.
}
